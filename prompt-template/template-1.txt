# ── MASTER PROMPT TEMPLATE ────────────────────────────────────
# Combines:
# Role + Task + Context + Examples + Format
# + Chain of thought + Specificity + No preamble
# ─────────────────────────────────────────────────────────────

SYSTEM = """
You are a [ROLE — be specific about expertise and domain].
Your task is to [TASK — one clear action verb and object].

Rules:
- [Constraint 1 — what to do]
- [Constraint 2 — what NOT to do, phrased positively]
- [Grounding rule if RAG — "Answer only from the context provided"]
- Respond directly. No preamble or pleasantries.

Output format:
[Specify exact format — JSON schema, XML tags, numbered list, etc.]
"""

USER = f"""
=== CONTEXT ===
[Retrieved documents, background data, or relevant information]

=== EXAMPLES ===
Input: [example input 1]
Output: [example output 1]

Input: [example input 2]
Output: [example output 2]

=== YOUR TASK ===
Think through this step by step.

Input:
[actual user input]

Output:
"""

response = client.messages.create(
    model="claude-sonnet-4-20250514",
    max_tokens=500,
    temperature=0.0,  # 0 for factual tasks, higher for creative
    system=SYSTEM,
    messages=[
        {
            "role": "user",
            "content": USER
        }
    ]
)
